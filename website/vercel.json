{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --dir .. run dev:prepare:lib && pnpm --dir .. run build && if [ -n \"$CONVEX_DEPLOY_KEY\" ]; then pnpm exec convex deploy --cmd 'pnpm exec nuxt build'; else pnpm exec nuxt build; fi"
}
