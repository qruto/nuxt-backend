---
title: "convex/integrations/ai"
description: "API reference for nuxt-backend/ai."
navigation:
  title: "ai"
  icon: i-lucide-server
---
<!-- Generated by TypeDoc (pnpm run docs:reference) — do not edit; change the JSDoc in src/ instead. -->

# convex/integrations/ai

**`Experimental`**

The rails for selling metered AI features — `nuxt-backend/ai`: wrap any
Convex action so it is rate-limited, prepaid-credit-metered (reserve → run →
settle — a failed run consumes nothing), and optionally token-streamed to
the browser with the text persisted server-side (reload mid-stream and it
continues).

Composes the billing spend reservation (`setupBilling`), the rate limiter,
and the upstream persistent-text-streaming component — usage recording IS
the provider event, so per-customer usage shows up in the provider's own
portal and dashboard with no extra tables here.

Experimental: the metering model (reserve/settle over provider meter
events) is the newest part of the package and may change shape in a minor
release; `useAiStream` on the Vue side is experimental with it.

## Interfaces

### AiRateLimiter

Defined in: [src/convex/integrations/ai.ts:48](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L48)

**`Experimental`**

The rate limiter shape `setupAi` consumes — satisfied by a
`setupRateLimiter(...)` instance, which seeds the `ai` and `aiBudget` limits
by default. Typed against the literal default names (the limiter's
conditional rest-tuple signature only resolves for known names — the same
pattern as `BillingRateLimiter`); custom `limit:` names pass through a cast
at the call site.

#### Methods

##### limit()

```ts
limit(
   ctx, 
   name, 
   options?
): Promise<{
  ok: boolean;
  retryAfter?: number;
}>;
```

Defined in: [src/convex/integrations/ai.ts:52](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L52)

**`Experimental`**

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | `RunWriteCtx` & \{ `auth?`: `Auth`; \} |
| `name` | `"ai"` \| `"aiBudget"` |
| `options?` | \{ `key?`: `string`; `throws?`: `boolean`; `count?`: `number`; `config?`: [`RateLimitConfig`](/api-reference/reference/convex/integrations/rate-limit#ratelimitconfig); \} |
| `options.key?` | `string` |
| `options.throws?` | `boolean` |
| `options.count?` | `number` |
| `options.config?` | [`RateLimitConfig`](/api-reference/reference/convex/integrations/rate-limit#ratelimitconfig) |

###### Returns

`Promise`\<\{
  `ok`: `boolean`;
  `retryAfter?`: `number`;
\}\>

***

### AiComponents

Defined in: [src/convex/integrations/ai.ts:60](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L60)

**`Experimental`**

The component handles `setupAi` reads from your generated `components` object.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="backend"></a> `backend` | \{ `ai`: \{ `createRequest`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; \}, `null`\>; `getByStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \| \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; `status`: `"reserved"` \| `"settled"` \| `"released"`; `createdAt`: `number`; \} \| `null`\>; `markSettled`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\>; `markReleased`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\>; \}; `billing`: \{ `finalize`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `finalAmount?`: `number`; \}, \{ `settled`: `boolean`; `released`: `number`; `balance`: `number`; `balanceBefore`: `number`; `releaseJobId?`: `string`; \}\>; `release`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; \}, `null`\>; `attachReleaseJob`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `jobId`: `string`; \}, `null`\>; \}; \} | **`Experimental`** | [src/convex/integrations/ai.ts:61](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L61) |
| `backend.ai` | \{ `createRequest`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; \}, `null`\>; `getByStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \| \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; `status`: `"reserved"` \| `"settled"` \| `"released"`; `createdAt`: `number`; \} \| `null`\>; `markSettled`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\>; `markReleased`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\>; \} | - | [src/convex/integrations/ai.ts:62](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L62) |
| `backend.ai.createRequest` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; \}, `null`\> | - | [src/convex/integrations/ai.ts:63](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L63) |
| `backend.ai.getByStream` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \| \{ `streamId`: `string`; `name`: `string`; `entityId`: `string`; `userId`: `string`; `args`: `string`; `meterId?`: `string`; `cost`: `number`; `externalId`: `string`; `status`: `"reserved"` \| `"settled"` \| `"released"`; `createdAt`: `number`; \} \| `null`\> | - | [src/convex/integrations/ai.ts:73](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L73) |
| `backend.ai.markSettled` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\> | - | [src/convex/integrations/ai.ts:85](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L85) |
| `backend.ai.markReleased` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`\> | - | [src/convex/integrations/ai.ts:86](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L86) |
| `backend.billing` | \{ `finalize`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `finalAmount?`: `number`; \}, \{ `settled`: `boolean`; `released`: `number`; `balance`: `number`; `balanceBefore`: `number`; `releaseJobId?`: `string`; \}\>; `release`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; \}, `null`\>; `attachReleaseJob`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `jobId`: `string`; \}, `null`\>; \} | The entitlement cache's reservation protocol. `setupAi` closes its own spends here rather than through `settleSpend`: only `finalize` can drop a reservation *and* hand back the unspent remainder atomically, and it reports the balance the low-balance hooks read. | [src/convex/integrations/ai.ts:94](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L94) |
| `backend.billing.finalize` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `finalAmount?`: `number`; \}, \{ `settled`: `boolean`; `released`: `number`; `balance`: `number`; `balanceBefore`: `number`; `releaseJobId?`: `string`; \}\> | - | [src/convex/integrations/ai.ts:95](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L95) |
| `backend.billing.release` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; \}, `null`\> | - | [src/convex/integrations/ai.ts:106](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L106) |
| `backend.billing.attachReleaseJob` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `userId`: `string`; `externalId`: `string`; `jobId`: `string`; \}, `null`\> | - | [src/convex/integrations/ai.ts:107](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L107) |
| <a id="persistenttextstreaming"></a> `persistentTextStreaming` | \{ `lib`: \{ `createStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ \}, `string`, `string` \| `undefined`\>; `addChunk`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `text`: `string`; `final`: `boolean`; \}, `null`, `string` \| `undefined`\>; `setStreamStatus`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `status`: `string`; `streamId`: `string`; \}, `null`, `string` \| `undefined`\>; `getStreamStatus`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, `string`, `string` \| `undefined`\>; `getStreamText`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \{ `text`: `string`; `status`: `string`; \}, `string` \| `undefined`\>; `deleteStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`, `string` \| `undefined`\>; \}; \} | **`Experimental`** | [src/convex/integrations/ai.ts:114](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L114) |
| `persistentTextStreaming.lib` | \{ `createStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ \}, `string`, `string` \| `undefined`\>; `addChunk`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `text`: `string`; `final`: `boolean`; \}, `null`, `string` \| `undefined`\>; `setStreamStatus`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `status`: `string`; `streamId`: `string`; \}, `null`, `string` \| `undefined`\>; `getStreamStatus`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, `string`, `string` \| `undefined`\>; `getStreamText`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \{ `text`: `string`; `status`: `string`; \}, `string` \| `undefined`\>; `deleteStream`: [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`, `string` \| `undefined`\>; \} | - |  |
| `persistentTextStreaming.lib.createStream` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ \}, `string`, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:1 |
| `persistentTextStreaming.lib.addChunk` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; `text`: `string`; `final`: `boolean`; \}, `null`, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:2 |
| `persistentTextStreaming.lib.setStreamStatus` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `status`: `string`; `streamId`: `string`; \}, `null`, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:7 |
| `persistentTextStreaming.lib.getStreamStatus` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, `string`, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:11 |
| `persistentTextStreaming.lib.getStreamText` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"query"`, `"internal"`, \{ `streamId`: `string`; \}, \{ `text`: `string`; `status`: `string`; \}, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:14 |
| `persistentTextStreaming.lib.deleteStream` | [`FunctionReference`](https://docs.convex.dev/api/modules/server#functionreference)\<`"mutation"`, `"internal"`, \{ `streamId`: `string`; \}, `null`, `string` \| `undefined`\> | - | node\_modules/@convex-dev/persistent-text-streaming/dist/component/lib.d.ts:20 |

***

### AiBudget

Defined in: [src/convex/integrations/ai.ts:128](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L128)

**`Experimental`**

A per-entity spending cap, enforced through the rate limiter as a fixed
window over spend *value*: each metered call consumes its cost in tokens, so
`{ units: 500, period: DAY }` is "500 credits a day per workspace", not "500
calls a day".

A cap, not a ledger — the window is the limiter's own state, there is no new
table, and (like every rate limit) consumption is never given back: a run
that fails, or finalizes under its estimate, still spent the estimate
against the window. Size the budget with that slack in mind.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="units"></a> `units` | `number` | **`Experimental`** Credits an entity may spend per window. | [src/convex/integrations/ai.ts:130](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L130) |
| <a id="period"></a> `period` | `number` | **`Experimental`** Window length in ms — `HOUR` / `DAY` from `nuxt-backend/rate-limit`. | [src/convex/integrations/ai.ts:132](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L132) |
| <a id="limit-1"></a> `limit?` | `string` | **`Experimental`** The named limit the window is recorded under. Default `'aiBudget'` (see `DEFAULT_LIMITS`); name your own to keep separate budgets per feature. | [src/convex/integrations/ai.ts:137](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L137) |

***

### CreditsBalanceEvent

Defined in: [src/convex/integrations/ai.ts:141](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L141)

**`Experimental`**

What crossed a balance threshold, handed to the low-balance hooks.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="entityid"></a> `entityId` | `string` | **`Experimental`** The billing entity (workspace/user) charged. | [src/convex/integrations/ai.ts:143](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L143) |
| <a id="userid"></a> `userId` | `string` | **`Experimental`** The member whose call spent the credits. | [src/convex/integrations/ai.ts:145](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L145) |
| <a id="meter"></a> `meter?` | `string` | **`Experimental`** The configured meter name, when the spend named one. | [src/convex/integrations/ai.ts:147](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L147) |
| <a id="meterid"></a> `meterId?` | `string` | **`Experimental`** The provider meter id. | [src/convex/integrations/ai.ts:149](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L149) |
| <a id="balance"></a> `balance` | `number` | **`Experimental`** Balance after the spend settled (negative when overage is allowed). | [src/convex/integrations/ai.ts:151](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L151) |
| <a id="previousbalance"></a> `previousBalance` | `number` | **`Experimental`** Balance as of before the spend reserved. | [src/convex/integrations/ai.ts:153](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L153) |
| <a id="spent"></a> `spent` | `number` | **`Experimental`** Credits this spend actually consumed. | [src/convex/integrations/ai.ts:155](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L155) |
| <a id="threshold"></a> `threshold?` | `number` | **`Experimental`** The configured threshold, for `onCreditsLow`. | [src/convex/integrations/ai.ts:157](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L157) |

***

### AiCreditsConfig

Defined in: [src/convex/integrations/ai.ts:169](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L169)

**`Experimental`**

Credit-side reactions to a settled spend.

The threshold lives here — on `setupAi` — rather than in
`appConfig.backend.*`: that is the client-side content layer (labels, plan
copy), and these hooks fire inside a Convex action where no Nuxt app config
exists. Keep them thin: send nothing from here (the lifecycle emails own
that), just record or hand off.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="lowbalancethreshold"></a> `lowBalanceThreshold?` | `number` | **`Experimental`** Fire [onCreditsLow](#oncreditslow) when a settle takes the balance from above this to at or below it. Omit to never fire it. | [src/convex/integrations/ai.ts:174](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L174) |
| <a id="oncreditslow"></a> `onCreditsLow?` | (`ctx`, `event`) => `void` \| `Promise`\<`void`\> | **`Experimental`** The balance just crossed [lowBalanceThreshold](#lowbalancethreshold) downwards. Fires on the crossing only — a second spend that stays below it is silent, so this is safe to wire straight to a notification. | [src/convex/integrations/ai.ts:180](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L180) |
| <a id="oncreditsexhausted"></a> `onCreditsExhausted?` | (`ctx`, `event`) => `void` \| `Promise`\<`void`\> | **`Experimental`** The balance just went from positive to zero-or-below. Same crossing rule. | [src/convex/integrations/ai.ts:182](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L182) |

***

### SetupAiConfig

Defined in: [src/convex/integrations/ai.ts:185](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L185)

**`Experimental`**

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="billing"></a> `billing` | [`Billing`](/api-reference/reference/convex/integrations/billing#billing) | **`Experimental`** The `setupBilling(...)` instance — supplies entity resolution + the credit spend. | [src/convex/integrations/ai.ts:187](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L187) |
| <a id="ratelimiter"></a> `rateLimiter?` | [`AiRateLimiter`](#airatelimiter) | **`Experimental`** Your `setupRateLimiter(...)` instance. Metered actions/streams check their named limit (default `'ai'`) per billing entity before reserving. Omit to leave them unthrottled. | [src/convex/integrations/ai.ts:193](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L193) |
| <a id="cors"></a> `cors?` | \{ `origin?`: `string`; \} | **`Experimental`** CORS origin for the stream endpoint. Defaults to the `SITE_URL` env var. | [src/convex/integrations/ai.ts:195](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L195) |
| `cors.origin?` | `string` | - | [src/convex/integrations/ai.ts:195](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L195) |
| <a id="budget"></a> `budget?` | [`AiBudget`](#aibudget) | **`Experimental`** A per-entity credit budget on top of the call-rate limit. Requires `rateLimiter` (it is enforced through it). | [src/convex/integrations/ai.ts:200](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L200) |
| <a id="credits"></a> `credits?` | [`AiCreditsConfig`](#aicreditsconfig) | **`Experimental`** Low-balance reactions fired after a spend settles. | [src/convex/integrations/ai.ts:202](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L202) |
| <a id="streamtimeout"></a> `streamTimeout?` | `number` \| `false` | **`Experimental`** How long a started stream may hold its reservation before it is released automatically (ms). Default 5 minutes; `false` disables the timer. | [src/convex/integrations/ai.ts:207](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L207) |

***

### MeteredUsage

Defined in: [src/convex/integrations/ai.ts:211](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L211)

**`Experimental`**

Usage details handed to metered handlers (`ctx.usage`).

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="entityid-1"></a> `entityId` | `string` | **`Experimental`** The billing entity (workspace/user) charged. | [src/convex/integrations/ai.ts:213](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L213) |
| <a id="userid-1"></a> `userId` | `string` | **`Experimental`** The signed-in caller. | [src/convex/integrations/ai.ts:215](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L215) |
| <a id="meter-1"></a> `meter?` | `string` | **`Experimental`** Configured meter name, when metered. | [src/convex/integrations/ai.ts:217](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L217) |
| <a id="cost"></a> `cost` | `number` | **`Experimental`** Credits this call costs — the estimate, for a finalized cost. | [src/convex/integrations/ai.ts:219](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L219) |

***

### FinalizedCost

Defined in: [src/convex/integrations/ai.ts:242](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L242)

**`Experimental`**

A cost that is only known once the work is done: reserve `estimate`, run,
then settle what `finalize` returns and hand the remainder back.

The estimate is the *ceiling* — a final amount above it is clamped down (the
reservation is what made the spend safe against a concurrent one, so
settling beyond it would be a second, unguarded debit). Estimate high.

Requires a sum meter (one with a `property`): the finalized amount rides in
the ingested event's metadata, and a count meter can only ever count 1 per
event.

#### Example

```ts
cost: {
  estimate: 500,
  finalize: result => priceTokens({ model: result.model, ...result.usage }, prices),
}
```

#### Type Parameters

| Type Parameter |
| ------ |
| `Args` *extends* `PropertyValidators` |
| `Result` |

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="estimate"></a> `estimate` | `number` \| ((`args`) => `number`) | **`Experimental`** Credits to reserve up front — a number or derived from the args. | [src/convex/integrations/ai.ts:244](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L244) |
| <a id="finalize"></a> `finalize` | (`result`, `usage`) => `number` | **`Experimental`** The credits actually consumed, from what the handler returned. | [src/convex/integrations/ai.ts:246](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L246) |

***

### MeteredActionConfig

Defined in: [src/convex/integrations/ai.ts:255](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L255)

**`Experimental`**

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Args` *extends* `PropertyValidators` | - |
| `Result` | `unknown` |

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="meter-2"></a> `meter` | `string` \| `false` | **`Experimental`** The configured credit meter to charge (`setupBilling({ credits })` / catalog key) — or `false` for a rate-limit-only action. | [src/convex/integrations/ai.ts:260](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L260) |
| <a id="cost-1"></a> `cost?` | [`MeteredCost`](#meteredcost)\<`Args`, `Result`\> | **`Experimental`** Credits per call. Default `1`. See [MeteredCost](#meteredcost). | [src/convex/integrations/ai.ts:262](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L262) |
| <a id="limit-2"></a> `limit?` | `string` \| `false` | **`Experimental`** Named rate limit (checked per billing entity via the configured `rateLimiter`). Default `'ai'`; `false` disables the check. | [src/convex/integrations/ai.ts:267](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L267) |
| <a id="allowoverage"></a> `allowOverage?` | `boolean` | **`Experimental`** Let this spend take the balance negative instead of refusing it — for a pay-as-you-go meter with no credit benefit behind it, where every unit is overage the provider invoices at the end of the cycle. | [src/convex/integrations/ai.ts:273](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L273) |
| <a id="charge"></a> `charge?` | `"success"` \| `"start"` | **`Experimental`** When to charge: `'success'` (default) settles after the handler returns — a failed run consumes nothing; `'start'` settles before running, for work that is irrevocably consumed upstream the moment it starts. A finalized cost needs the result, so it only applies to `'success'`. | [src/convex/integrations/ai.ts:280](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L280) |
| <a id="args-3"></a> `args` | `Args` | **`Experimental`** | [src/convex/integrations/ai.ts:281](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L281) |
| <a id="handler"></a> `handler` | (`ctx`, `args`) => `Promise`\<`Result`\> | **`Experimental`** | [src/convex/integrations/ai.ts:282](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L282) |

***

### MeteredStreamConfig

Defined in: [src/convex/integrations/ai.ts:285](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L285)

**`Experimental`**

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Args` *extends* `PropertyValidators` | - |
| `Result` | `unknown` |

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="name"></a> `name` | `string` | **`Experimental`** Registry key — unique per app; the HTTP dispatcher routes by it. | [src/convex/integrations/ai.ts:287](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L287) |
| <a id="meter-3"></a> `meter` | `string` \| `false` | **`Experimental`** The configured credit meter to charge, or `false` for limit-only. | [src/convex/integrations/ai.ts:289](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L289) |
| <a id="cost-2"></a> `cost?` | [`MeteredCost`](#meteredcost)\<`Args`, `Result`\> | **`Experimental`** Credits per stream. Default `1`. A [FinalizedCost](#finalizedcost) prices what the handler *returns* — return the model's token usage from it. | [src/convex/integrations/ai.ts:294](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L294) |
| <a id="limit-3"></a> `limit?` | `string` \| `false` | **`Experimental`** Named rate limit. Default `'ai'`; `false` disables. | [src/convex/integrations/ai.ts:296](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L296) |
| <a id="allowoverage-1"></a> `allowOverage?` | `boolean` | **`Experimental`** Let this spend take the balance negative (pay-as-you-go meters). | [src/convex/integrations/ai.ts:298](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L298) |
| <a id="args-5"></a> `args` | `Args` | **`Experimental`** | [src/convex/integrations/ai.ts:299](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L299) |
| <a id="handler-1"></a> `handler` | (`ctx`, `args`, `stream`) => `Promise`\<`Result`\> | **`Experimental`** Produce the stream: `append(text)` pushes a chunk to the client AND persists it. Credits settle when the handler finishes; an error or disconnect releases them — an interrupted stream never charges. | [src/convex/integrations/ai.ts:305](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L305) |

***

### Ai

Defined in: [src/convex/integrations/ai.ts:312](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L312)

**`Experimental`**

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="meteredaction"></a> `meteredAction` | \<`Args`, `Result`\>(`config`) => `RegisteredAction`\<`"public"`\> | **`Experimental`** A drop-in replacement for `action` that rate-limits, reserves prepaid credits, runs your handler, then settles the spend (usage lands in the billing provider as an ingested event). Failed runs release the reservation — nothing charged. **Examples** `export const generate = ai.meteredAction({ meter: 'credits', cost: args => args.long ? 5 : 1, args: { prompt: v.string(), long: v.optional(v.boolean()) }, handler: async (ctx, { prompt }) => runModel(prompt), })` **Token pricing — reserve an estimate, settle the actual usage** `export const generate = ai.meteredAction({ meter: 'credits', cost: { estimate: 200, finalize: result => priceTokens(result.usage, prices), }, args: { prompt: v.string() }, handler: async (ctx, { prompt }) => runModel(prompt), })` | [src/convex/integrations/ai.ts:342](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L342) |
| <a id="stream"></a> `stream` | \<`Args`, `Result`\>(`config`) => \{ `start`: `RegisteredAction`\<`"public"`\>; `body`: `RegisteredQuery`\<`"public"`\>; \} | **`Experimental`** A metered, persisted token stream. Returns `{ start, body }` to re-export from your module — `useAiStream` drives them: `start` reserves credits and creates the stream, the HTTP endpoint (`registerBackendRoutes({ ai })`) runs your handler and streams chunks, `body` serves the persisted text reactively (reload mid-stream and it continues). A started stream that is never delivered (the browser closed before the HTTP call, or it died mid-flight) holds its reservation until the scheduled auto-release hands the credits back — `streamTimeout`. | [src/convex/integrations/ai.ts:354](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L354) |
| <a id="httphandler"></a> `httpHandler` | `PublicHttpAction` | **`Experimental`** The stream endpoint for `registerBackendRoutes({ ai })` (POST + CORS preflight). | [src/convex/integrations/ai.ts:359](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L359) |
| <a id="corsorigin"></a> `corsOrigin` | `string` | **`Experimental`** CORS headers for the stream route (used by `registerBackendRoutes`). | [src/convex/integrations/ai.ts:361](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L361) |

***

### ModelPrice

Defined in: [src/convex/integrations/ai.ts:390](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L390)

**`Experimental`**

What one model charges, in credits per [ModelPrice.per](#per) tokens.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="input"></a> `input` | `number` | **`Experimental`** Credits per block of input (prompt) tokens. | [src/convex/integrations/ai.ts:392](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L392) |
| <a id="output"></a> `output` | `number` | **`Experimental`** Credits per block of output (completion) tokens. | [src/convex/integrations/ai.ts:394](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L394) |
| <a id="cachedinput"></a> `cachedInput?` | `number` | **`Experimental`** Credits per block of cache-read input tokens, when the model prices them separately. Omit and cached tokens are billed as ordinary input. | [src/convex/integrations/ai.ts:399](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L399) |
| <a id="per"></a> `per?` | `number` | **`Experimental`** Tokens each rate covers. Default 1; use `1_000_000` for per-million rates. | [src/convex/integrations/ai.ts:401](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L401) |

***

### TokenUsage

Defined in: [src/convex/integrations/ai.ts:408](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L408)

**`Experimental`**

A model call's token counts, as the SDKs report them.

#### Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="model"></a> `model` | `string` | **`Experimental`** The model id — the key looked up in the price table. | [src/convex/integrations/ai.ts:410](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L410) |
| <a id="input-1"></a> `input` | `number` | **`Experimental`** Input (prompt) tokens billed at the full rate. | [src/convex/integrations/ai.ts:412](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L412) |
| <a id="output-1"></a> `output` | `number` | **`Experimental`** Output (completion) tokens. | [src/convex/integrations/ai.ts:414](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L414) |
| <a id="cachedinput-1"></a> `cachedInput?` | `number` | **`Experimental`** Cache-read input tokens, when the model reports them separately. | [src/convex/integrations/ai.ts:416](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L416) |

## Type Aliases

### MeteredCost

```ts
type MeteredCost<Args, Result> = 
  | number
  | ((args) => number)
| FinalizedCost<Args, Result>;
```

Defined in: [src/convex/integrations/ai.ts:250](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L250)

**`Experimental`**

Credits per call: fixed, derived from the args, or estimated then finalized.

#### Type Parameters

| Type Parameter |
| ------ |
| `Args` *extends* `PropertyValidators` |
| `Result` |

***

### TokenPriceTable

```ts
type TokenPriceTable = Record<string, ModelPrice>;
```

Defined in: [src/convex/integrations/ai.ts:405](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L405)

**`Experimental`**

The app's price list: model id → [ModelPrice](#modelprice).

## Functions

### priceTokens()

```ts
function priceTokens(
   usage, 
   table, 
   options?
): number;
```

Defined in: [src/convex/integrations/ai.ts:440](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L440)

**`Experimental`**

Price a model call in credits from its token usage and a price table the app
owns. No provider knows what a model costs you — rates change weekly and
only your margin decides the credit price — so the table is yours; this is
the arithmetic, pure and testable.

Rates are credits per `per` tokens (default 1). Express per-million pricing
as `per: 1_000_000` on the entry or as a table-wide `options.per`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `usage` | [`TokenUsage`](#tokenusage) |
| `table` | [`TokenPriceTable`](#tokenpricetable) |
| `options` | \{ `per?`: `number`; `round?`: `"up"` \| `"nearest"` \| `"none"`; `minimum?`: `number`; \} |
| `options.per?` | `number` |
| `options.round?` | `"up"` \| `"nearest"` \| `"none"` |
| `options.minimum?` | `number` |

#### Returns

`number`

#### Example

```ts
const prices = {
  'fast-1': { input: 3, output: 15, per: 1_000_000 },
  'deep-1': { input: 15, output: 75, cachedInput: 1.5, per: 1_000_000 },
}
priceTokens({ model: 'fast-1', input: 12_000, output: 800 }, prices) // → 0.048
```

#### Throws

when the model is missing from the table — a silent 0 would be a
revenue leak, and an unpriced model is a deployment mistake worth surfacing.

***

### setupAi()

```ts
function setupAi(components, config): Ai;
```

Defined in: [src/convex/integrations/ai.ts:482](https://github.com/qruto/nuxt-backend/blob/main/src/convex/integrations/ai.ts#L482)

**`Experimental`**

Configure the AI rails over billing + rate limiting + streaming.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `components` | [`AiComponents`](#aicomponents) |
| `config` | [`SetupAiConfig`](#setupaiconfig) |

#### Returns

[`Ai`](#ai)

#### Example

```ts
// backend/ai.ts
import { setupAi } from 'nuxt-backend/ai'
import { components } from './_generated/api'
import { billing } from './billing'
import { rateLimiter } from './rateLimiter'

export const ai = setupAi(components, { billing, rateLimiter })
export const generate = ai.meteredAction({ ... })
export const { start: startEcho, body: echoBody } = ai.stream({ name: 'echo', ... })
```
