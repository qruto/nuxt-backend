---
title: Architecture
description: How the Convex client, Nuxt server, and same-origin auth proxy fit together.
navigation:
  icon: i-lucide-network
---

The package ships two things in one:

| Layer | What it does |
|---|---|
| **Nuxt module** (`nuxt-backend`) | Registers plugins, composables, server utilities, the auth proxy, and auto-scaffolds Convex root files |
| **Convex component** (`nuxt-backend/component/convex.config`) | Defines the all-in-one `backend` component: the Better Auth adapter + auth tables, transactional email (the provider component nested inside), the billing entitlement cache, gift purchases, the AI metering records, and the webhook delivery log |

The scaffolded `convex.config.ts` mounts the `backend` component plus the six upstream `@convex-dev/*` components (`aggregate`, `migrations`, `persistentTextStreaming`, `polar`, `rateLimiter`, `workflow`) at the app level — the seventh, `resend`, is mounted inside the `backend` component — one explicit `app.use` each — see the [component tree](/platform/overview).

## Request flows

Three transports run side by side, each with its own path through the stack:

::card-group
  :::card{icon="i-lucide-zap" title="Real-time"}
  `Browser ⇄ Convex Cloud` over a **WebSocket** (`ConvexVueClient`) — queries subscribe, Convex pushes live updates.
  :::

  :::card{icon="i-lucide-server" title="SSR"}
  `Browser → Nuxt → Convex` over HTTP (`ConvexHttpClient`) — the server renders HTML and ships a hydration payload.
  :::

  :::card{icon="i-lucide-shield-check" title="Auth"}
  `Browser → /api/auth/* → Nuxt proxy → Better Auth → Convex` — the session token and `Set-Cookie` return same-origin.
  :::
::

### Real-time data

The client `ConvexVueClient` connects over a **WebSocket** for real-time reactivity. `useQuery`, `useMutation`, `useAction`, and pagination subscribe through this client and update live.

::demo-connection
::

### SSR

On the server, `fetchQuery`, `fetchMutation`, `fetchAction`, and `preloadQuery` use a `ConvexHttpClient` under the hood. Pass `{ token }` when authentication is required. `preloadQuery` produces a payload that hydrates seamlessly into the live client via `usePreloadedQuery`.

### Authentication

`/api/auth/*` is **proxied** from Nuxt to the Convex site URL so cookies stay same-origin. Better Auth persists users and sessions through the Convex adapter inside the `backend` component, and issues a session token the client uses to authenticate its WebSocket.

### Agents and inbound webhooks

Beyond those three, two more paths end in the backend without a browser in front:

- **Agents** — `Agent → /mcp (Nitro, OAuth gate) → POST <site URL>/mcp/exchange → Convex`. The gate exchanges the agent's Bearer token for a Convex-authenticated session, and the tools run as the user who consented. On by default; `backend: { mcp: false }` removes it. See [MCP server](/agents/mcp-server).
- **Inbound webhooks** — `Provider → <site URL>/billing/events | /email/events → Convex HTTP router`. `registerBackendRoutes` in the scaffolded `http.ts` mounts both (and the metered AI stream at `/ai/stream`); every delivery is signature-verified, deduped, logged, and fail-closed (`503`) until its secret is set. See [Webhooks](/platform/webhooks).

::note
This mirrors the official Convex + Better Auth integration: real-time over WebSocket, SSR over HTTP, and a same-origin auth proxy keeping cookies on your domain.
::
