# Dependencies
node_modules
pnpm-debug.log*

# Logs
*.log*

# Temp directories
.temp
.tmp
.cache

# Generated dirs
dist
# The website's Convex codegen is committed (a Vercel preview builds without a
# deployment; CI regenerates it and fails on a diff) — all but the AI
# guidelines the CLI drops beside it.
website/backend/_generated/ai/

# Nuxt
.nuxt
.output
.data
.vercel_build_output
.build-*
.netlify

# Env
.env
.env.*
!.env.example

# Testing
reports
coverage
*.lcov
*.junit.xml
.nyc_output

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Intellij idea
*.iml
.idea

# Build artifacts
*.tsbuildinfo
*.tgz

# Bundle analysis (Sonda)
.sonda

# deepsec security-scanner workspace (kept local-only: its INFO.md maps the
# repo's security-sensitive surface and exported findings describe unfixed
# vulnerabilities — neither belongs in a public repo)
.deepsec

# OSX
.DS_Store
.AppleDouble
.LSOverride
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Claude Code local settings (machine-specific permissions) and its scratch
# writes
.claude/settings.local.json
.claude/.cc-writes/

# fallow cache and the fallow GitHub Action's outputs (typos once failed on them)
.fallow/
fallow-*

# QA screenshots (local-only)
.qa
*-dev.log
